# Память проекта переехала в `.memory/`

Проектная память живёт в `.memory/` — туда пишет нативный memory-tool Claude Code
(`~/.claude/projects/<slug>/memory` — симлинк на этот каталог).

Точка входа — `.memory/MEMORY.md`.
