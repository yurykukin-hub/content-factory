# Content Factory — Memory

## Ключевые факты
- [2026-06-27] ✅ **Тесты на критичные пути (Verification Debt, P1 аудита)** — +55 тестов (283→338, `019d253`), все моки, БЕЗ живых вызовов VK/IG/ЮKassa/OpenRouter, CI backend-tests зелёный. `billing.ts`: наценка/USD→RUB (копейки, ceil в пользу владельца), `chargeUser` (атомарное списание с WHERE-гардом от гонки, ADMIN-исключение, недостаток средств→throw), `topUp`, `canAfford`. `publish-runner.ts`: `publishPostVersion`/`schedulePostVersion` — оркестрация публикации, **getPublisher замокан → 0 живых вызовов**; booking-URL только в VK-ленту + идемпотентность; вырезание инлайн-хэштегов (только если массив тегов непуст); статусы PUBLISHED/FAILED + rollup поста + события + ошибки. `igFeedTargetSize` (IG crop 0.8…1.91), `calculateCost` (тарифы моделей = база биллинга). Файлы: `services/__tests__/{billing,publish-runner}.test.ts`, `services/publishers/__tests__/postmypost-crop.test.ts`, `services/ai/__tests__/openrouter-cost.test.ts`.
- [2026-06-27] ✅ **backend tsc strict** (P1 CI baseline→strict): `backend npx tsc --noEmit` 23→0 (`13a54db`), снят `continue-on-error` у job `backend-typecheck` → **гейт блокирующий**, CI зелёный (3/3 jobs), 283 теста зелёные. **НЕ «Hono c.get()»** (те ушли в security-сессии 26.06) — реально **Prisma-input дрейф** + точечное, ВСЕ 23 проверены `eng-code-reviewer` = SAFE (рантайм не меняется, баги не маскируются): JSON Zod-поля `z.unknown()`→`z.any()` (businesses examplePosts/links, platforms config — под `InputJsonValue`, валидация идентична); касты `data` к `Prisma.CharacterUncheckedCreate/UpdateInput`+`GenerationSessionCreateInput` (снимают неоднозначность checked/unchecked при сыром FK referenceMediaId/personaId рядом с nested write; объект реально валиден в рантайме); `video-poller batchTaskIds: null`→`Prisma.DbNull` (единств. читатель `Array.isArray` → эквивалентно); `auto-post a.platform as Platform`; мёртвое `ai.ts usage: result.usage` удалено (AiCompleteResult без usage → всегда undefined → JSON и так опускал); **`ai.ts config.port`→`config.PORT`** (config.port не существовал → dev-URL был `localhost:undefined`; prod на isProd-https-ветке не затронут); `version_created` добавлен в union `eventBus`; `skipOverlay`/`photoPosition` в локальный тип vk `storiesOptions` (есть в base.ts, реально передаются); Buffer/Blob + `Set<string>` касты. ✅ **Задеплоено 27.06** (`scripts/deploy.sh`: rsync dev→/opt/content-factory + rebuild backend, health `status:ok`, content.yurykukin.ru 200, миграции no-op — схема не менялась; `.env.prod`/uploads исключены rsync'ом).
- [2026-06-23] **UX-сессия CF — финал (edit-in-place + доработки дайджеста/сторис, ВСЁ В ПРОДЕ, ~21 коммит).** Поверх записи ниже:
  - **Override-текст: EDIT-IN-PLACE вместо bottom-sheet** (sheet не зашёл Юрию → откатили). Новый `components/posts/preview/EditableText.vue` (textarea «в стиле макета»: тот же шрифт/размер, без рамки). Превью IG/VK/TG + обёртка `PostPreview` принимают `editable`+`@update:text` → текст правится ПРЯМО в превью «как в ленте». Редактор (`PostEditorView`: кнопка «Текст»/«Готово» раскрывает inline-превью editable) И дайджест (`DigestView`: кнопка «Текст» у «Заменить фото»; per-task `adaptations`, autosave `PATCH /auto-posts/:id`). `ChannelOverrideSheet.vue` больше НЕ используется (кандидат на удаление).
  - **Дайджест — порядок:** «В редактор» (`open-editor`, keepProposed — предложение НЕ теряется) для ВСЕХ типов; вынесена ПОД превью по центру; **«Отклонить» УБРАНА** (есть авто-архив вчерашних proposed); «Поправить кадр» → в ряд медиа; «Текст» → у «Заменить фото»; **единый порядок табов превью VK→TG→IG** (`orderedPreviews`; раньше как агент создал → у сторис/постов по-разному); IG-превью центрировано (`mx-auto`) + кнопки под ним по центру.
  - **Сторис-превью — раскладка как в реале** (по скринам Юрия): нижний UI ПОД фото на тёмной полосе (не поверх). IG — поле «Отправить сообщение»+иконки под фото; VK — нативная кнопка «Забронировать» ПОВЕРХ фото справа внизу (тонкая) + полоса просмотров/иконок под фото. Верх (прогресс+аватар) — поверх фото.
  - **Умный «Назад»:** из дайджеста → «Назад в дайджест», из контента → «к контенту» (query `?from=digest`, переживает F5; `PostEditorView`+`StoryEditorView`).
  - **Урок:** UI проверять ВИЗУАЛЬНО, не «вслепую» (Юрий ловил: квадрат→4:5, мыло thumb, sheet vs edit-in-place, сторис-UI поверх фото вместо под). playwright на проде был недоступен → проверял Юрий с телефона.
- [2026-06-23] **UX-апгрейд композера/дайджеста — 5 задач, ВСЁ В ПРОДЕ (автономный прогон, пока Юрий спал; 5 коммитов/деплоев, 266 тестов).** (1) **Сторис-превью «как в соцсети»** — `StoriesPreview` дорисован до нативной рамки (прогресс-бар, аватар, поле «отправить сообщение»/иконки IG; кнопка-таблетка/лайк VK; baked-режим рисует только рамку без дубля текста). (2) **Медиа в редакторе** (`MediaUpload`): клик по фото → lightbox; меню «⋯» всегда видно (чинит тач); **кнопка «удалить фон» убрана из ВСЕЙ системы** (MediaUpload/MediaLibraryView/StoryEditorView/PsGallery/PhotoStudioView; backend rembg оставлен мёртвым). (3) **Override-текст** → новый `components/posts/ChannelOverrideSheet.vue` (bottom-sheet: табы каналов, мастер/свой текст, живое превью, AI-адаптация, лимит, сброс) ВМЕСТО тесного inline; используется в редакторе (`PostEditorView`) И дайджесте. (4a) **Дайджест «В редактор» больше НЕ убирает предложение**: `approveDigestTask({keepProposed})` + идемпотентность (без дублей Post); `POST /auto-posts/:id/open-editor`; `GET /auto-posts` обогащает превью из связанного Post → правки видны в карточке. (4b) **Правка текста прямо в дайджесте** — кнопка «Текст» открывает тот же sheet (per-канал adaptations); `PATCH /auto-posts/:id` принимает proposedText/proposedTags/adaptations + синк в связанный Post. (4c) Модалка публикации — формулировка яснее (выбор одного/нескольких каналов уже работал — чекбоксы). **На проверку Юрию (с телефона):** сторис-превью; lightbox/меню фото + нет «удалить фон»; override-sheet (редактор+дайджест); «В редактор» из дайджеста → назад → предложение на месте изменённым; правка текста кнопкой «Текст».
- [2026-06-23] **Карусель: reorder фото (drag-drop + стрелки) в редакторе поста — прод.** Несколько фото в посте можно переупорядочить; порядок = порядок в IG-карусели / VK·TG-альбоме при публикации. **Backend:** `POST /posts/:id/media/reorder` (`sortOrder` в `$transaction`, паттерн из `characters.ts` reorder, `where postId` — защита от чужих id). `publish-runner` уже публикует `orderBy sortOrder asc` → код публикации НЕ менялся. **Frontend:** `MediaUpload.vue` — карточки `draggable` (desktop HTML5 drag) + стрелки ◀▶ (мобильный, надёжнее капризного touch-drag) + номер кадра; `emit('reorder', ids)` → `PostEditorView.onMediaReorder` (оптимистичный локальный reorder + сохранение). Фикс: конфликт имён `onDrop` (drop-zone загрузки vs reorder) → reorder-хендлер переименован в `onCardDrop`. **Объём Ф1 (решение Юрия):** только редактор; агент дайджеста пока даёт 1 фото — карусель от агента = Ф2 (нужна миграция `AutoPostTask` на массив медиа + переделка approve-publish/`pickPhotoForPost`).
- [2026-06-23] **Фикс: белые поля у IG-фото — кроп в 4:5 на стороне CF (в репо, НЕ задеплоено, ждёт команду).** Юрий запостил фото вживую: VK-лента корректно, IG-лента с белыми полями. Причина — ограничение IG: лента допускает соотношение w/h только 0.8 (4:5) … 1.91 (1.91:1); вертикальное фото выше 4:5 → IG/Postmypost вписывает с белым фоном. CF слал оригинальные байты на все площадки одинаково. **Фикс — только в `publishers/postmypost.ts`** (единая воронка всех IG-путей: ручная/scheduler/дайджест): `uploadMedia` для IG-ленты (`publication_type=POST`) прогоняет фото через `normalizeForInstagramFeed` — sharp `.rotate()` (EXIF в пиксели ДО чтения w/h) → кроп `fit:'cover', position:'attention'` к ближайшей границе (4:5 для высоких / 1.91:1 для широких), **без апскейла**, fail-safe на оригинал при ошибке sharp. Чистая `igFeedTargetSize(w,h,forceRatio?)` вынесена (экспорт) + 6 юнит-тестов. Карусель (>1 фото) → общий 4:5 (IG требует одинаковое соотношение). **НЕ трогаются:** VK/TG (свои паблишеры), VK-via-PMP (гейт `isInstagram`), IG-сторис/Reels (≠POST), фото уже в норме (`changed=false`). 266 тестов, typecheck postmypost 0 ошибок, миграций нет. Стратегия «кроп» — решение Юрия (альтернатива «фото на размытом фоне» — на будущее через `AppConfig ig_feed_fit_mode`). **После деплоя:** перегенерировать/переопубликовать тестовое вертикальное фото и проверить ВИЗУАЛЬНО (нет полей в IG-ленте; VK и IG-сторис не изменились). **Бэкенд-фикс задеплоен на прод 2026-06-23** (проверено: боевая `igFeedTargetSize` кропает 9:16→4:5 на реальных фото). **+ Превью (UI):** IG-превью в дайджесте и композере (`InstagramPreview.vue`) теперь показывает РЕАЛЬНЫЙ формат после кропа — `aspectRatio` берётся из натуральных размеров фото с clamp 0.8…1.91 (а не квадрат `aspect-square`); фото грузится оригиналом (квадратный thumb врёт о соотношении). Цепочка: `DigestView`/`PostEditorView` → `PostPreview` → `InstagramPreview`. **+ Карусель:** при нескольких фото IG-превью листается как в Instagram (стрелки ‹›, свайп на мобильном, точки-индикаторы, счётчик N/M); все слайды показываются в 4:5 (бэкенд форсит общий ratio карусели). VK/TG-превью (`VkPreview`/`TelegramPreview`): одиночное фото — в РОДНОМ соотношении + грузится ОРИГИНАЛ (VK/TG формат не форсят, не как IG; чинит «квадрат + мыло» от thumb 400×400), несколько фото — сетка-коллаж (так VK/TG раскладывают альбом). Soft-clamp 0.5…2.5.
- _(06-03…06-08 факты: метрика, SMM-аналитика, Postmypost, каналы, отложка, форматы, инциденты — перенесены в `MEMORY-ARCHIVE.md`)_

## Блокеры
- [2026-06-05] VK scope `photos` + `stats` застряли (требуют модерации VK-приложения — ре-авторизация их НЕ даёт, выдаёт только wall/video/stories/offline). `photos` ОБОЙДЁН через Postmypost (постинг). `stats` (сторис/account-статы) — Юрий подаёт VK-апп на модерацию; пост-метрики VK уже идут через `wall.getById` (scope wall есть).

## Открытые задачи (next — что НЕ сделано / продолжить)
### Next (после ритма 2026-06-22)
- **✅ Набор инструкторов в SMM — В ПРОДЕ + LIVE (2026-06-22).** Еженедельный VK-пост-вакансия (по СРЕДАМ): `buildDigestRecruitmentPrompt` + `runRecruitmentPost` (PHOTO, фото команды) + инжект в утренний прогон ритма (`digest_recruitment_day=3`, `digest_recruitment_enabled=true`, каналы `digest_recruitment_channels=VK` — ТОЛЬКО VK, в IG вакансию не постим: не целевой; `digest_recruitment_contact`). **Телефон НаWоде = +7 921 323-06-16** (НЕ Фонтанка `636-66-74`). Smoke ОК: PHOTO/VK/фото/телефон в тексте. Human-in-the-loop (одобрение в /digest).
- **✅ key-facts агента — В ПРОДЕ + LIVE (2026-06-22).** `digest_key_facts` (AppConfig) дописывается к `brandContext` (одна точка → все промпты дайджеста) → агент ВСЕГДА знает важные факты бизнеса и не вводит в заблуждение. Выставлен факт **Tapiola→скала:** прокат/уроки со СКАЛЫ (не на базе Тапиолы — поток + вход закрыт), туры «Место силы» с ПРИЧАЛА Тапиолы (редкие). Только публично-безопасное (агент доков НЕ читает — я выжал из `~/.claude/knowledge/nawode-docs/tapiola-negotiation-2026.md`, секреты не кладу). Отдельного RAG-по-докам у агента нет — только этот курируемый канал.
- **Фаза 3 — промо-автопостинг:** скидки из ERP (`price_lists.discount_days` вт/пт + `service_slot_overrides.discount_price` разовые Светы + `promo_codes`) + погодный flash («распогодилось» + пустые слоты → −X% сегодня) через роли сторис; `getActiveDiscounts` в DataSourceAdapter; защита маржи `getRedLine` (плейбук). 
- **Фаза 4 — конкуренты+вдохновение:** расширить VK-список (`seed-competitors.ts`/UI); доска вдохновения IG (ссылка на рилс → AI-разбор по структуре плейбука → банк идей). IG-референсы: sup4you.ru, sup_fitness__, progress.ride, friends.sup.
- Тесты промо-механик из плейбука: ① «второй за 1₽», слот-филл-добивка, «Кадр недели».
### Свежие (сессия 2026-06-19/21 — медиатека + дайджест + Stories)
- **🔴 ПРОВЕРИТЬ реальную публикацию вживую (за Юрием):** прямая публикация сторис/фото из дайджеста («Опубликовать») и из редактора Stories НИ РАЗУ не тестировалась в VK/IG (необратимо — я не жал кнопку). Сделать первую реальную публикацию → убедиться: VK Stories + IG лента/сторис реально публикуются, текст/фото/каналы корректны. Это единственное непроверенное звено всей цепочки.
- **temp/weather при «Поправить кадр»:** `StoryDesignModal` получает только title/cta, НЕ temp/weather (`AutoPostTask` их не хранит) → при ручном переоформлении кадра из дайджеста погодный виджет пропадёт. Фикс: сохранять temp/weather в AutoPostTask ИЛИ render-design дотягивает погоду из `getDailySummary` для НаWоде (в редакторе baked — то же).
- **Legacy baked-сторис (до миграции `sourceMediaId` 2026-06-20):** «Поправить кадр» показывает дубль текста в превью (берёт baked вместо оригинала, т.к. sourceMediaId=null). Новые сторис — ок. Не критично (старые вытеснятся).
- **Drag по горизонтали для вертикальных фото:** не двигает (нет горизонтального запаса при cover) — это поведение CSS, не баг. Эффект на широких/квадратных фото.

### Из прошлых сессий (SMM-петля Ф2c+)
- **Карусель (Ф2c.2-3):** есть только рендер слайдов (`renderAndSaveCarousel`). НЕТ: UI создания карусели, агент-слайды, публикация серией, свайп-фото. `VK clickable_stickers` (Ф2c.4 — авто хештег/упоминание в сторис).
- **🎯 nawode-erp виджет Метрики (ГЛАВНОЕ — замкнёт SMM-ROI до денег):** счётчик 92916147 НА erp.nawode.ru + cross-domain + цели-шаги воронки (открыл→слот→submit/оплата) + Webvisor. Промпт готов: `~/.claude/plans/nawode-erp-metrika-prompt.md`. Готово к заходу: токен read+WRITE, SSH `root@155.212.219.88:/opt/nawode-erp`, продукты/URL брони. Затем CF: `bun src/set-business-metrika.ts nawode 92916147 <booking_goalId>`.
- **Утренний пинг дайджеста в TG:** код готов (`telegram-approval.ts`, graceful), ДОРМАНТ — нужны `telegram_approval_bot_token`+`telegram_approval_chat_id` (бот @BotFather, действие Юрия). **MAX-мессенджер доставка — НЕ реализована** (отдельный адаптер по образцу telegram-approval).
- **C1 (security, pre-existing):** `GET /businesses/:id/platforms` отдаёт `accessToken` (VK/TG/PMP токены) во фронт → сериализатор-маска + фронт write-only (фронт-импакт `BusinessDetailView.vue:246`).
- **VK scope stats/photos:** Юрий подаёт VK-апп на модерацию → VK сторис/account-статы + прямой VK фото-постинг (сейчас обход через Postmypost).
- **Дизайн-слой (06-12):** живой визуальный тест canvas-UI (залит в прод, глазами не проверен) + интеграция в композер постов + бренд-кит шаблоны.
- **«Вау»-редактор** (нужно дизайн-направление Юрия) + per-business Метрика UI-форма (счётчик/цели/токен в проекте).

## strategy-as-data + DataSourceAdapter (Эпик B Phase 2/3, 2026-06-04)
- **strategy.ts** (generic): getRubricNames, getOccasionsInRange, getStrategyBlock, getSeasonHint — рубрики/поводы/стратегия/сезон из БД per-business
- **Модели:** Rubric (name/sortOrder/isActive), Occasion (monthDay/topic/rubric/postType), BrandProfile.contentStrategy/seasonHints
- **Сидер:** `bun src/seed-nawode-strategy.ts` (идемпотентный, не затирает кастомизацию; seed=nawode-strategy.ts константы)
- **datasource/**: DataSourceAdapter интерфейс + NawodeErpAdapter (lazy-import nawode-data, обёртка) + NullAdapter + getDataSourceAdapter(business) по erpType
- **Добавить бизнес = данные (сидер/UI) + адаптер**, не ветки `if erpType` в коде
- HTTP-контракт `GET /public/daily-summary` (ERP-сторона, auth Business.erpApiKey) + NawodeHttpAdapter — Deferred (кросс-репо nawode-erp)

## SaaS-Ready (2026-04-11)
- RBAC (UserBusiness), Section Access (14 секций), refresh tokens, health endpoint
- Security: resource-access, CSRF X-Tab-ID, rate limiting, path traversal, Zod, 167 тестов

## Photo Studio (2026-04-18, updated 19.04)
- GenerationSession type='photo', 5 полей (photoModel, photoResolution, batchSize, photoAspectRatio, batchTaskIds)
- Модели: nano-banana-2 ($0.04-0.09) + nano-banana-pro ($0.07-0.12) + gpt-image-2. Batch 1/2/4. 10 aspect ratios. 3 resolutions
- Routes: /api/photos/* (generate, enhance-prompt 8 modes, agent-chat, edit-image, remove-bg)
- Frontend: PhotoStudioView + 7 Ps* компонентов. 50/50 layout, KeepAlive, SSE, auto-save
- Reference images: VideoStudio-style UI (56x56 thumbs, @N метки, dropdown загрузить/медиатека, preview popup с AI-описание). MediaPickerModal multi-select. До 14 refs NB2, 8 Pro
- **ВАЖНО:** Generate payload — фронтенд шлёт `model`/`resolution`/`aspectRatio` (НЕ photoModel/photoResolution/photoAspectRatio)

## AI Agent + Sound Studio + Voice Input + Reference System v2 — детали в CLAUDE.md
- AI Agent (Haiku/Sonnet, chatHistory per-session). Sound Studio (suno.ts, 13 Ss*, MusicPersona voice clone, 8 enhance modes). Voice Input (Whisper STT, AppConfig openai_api_key).
- Reference: **CharacterImage** (галерея фото на персонажа) + SharedCharacterCarousel/SharedRefModal + @CharName автокомплит + Character Sheet (4 views). ВАЖНО: Character.referenceMediaId/additionalAngles НЕ удалены (backward compat).

## Prisma Migrations — КРИТИЧНЫЕ ПРАВИЛА (2026-04-20)
- **НИКОГДА** не использовать `db push` на dev-БД с историей миграций. Только `bunx prisma migrate dev --name описание`
- **НИКОГДА** не редактировать SQL файл миграции ПОСЛЕ apply (ломает checksum → drift)
- Если миграция упала "already exists" → `bunx prisma migrate resolve --applied имя_миграции` (НЕ правка SQL)

## Видео-сторис + дизайн-слой (ffmpeg/sharp overlay) — детали в CLAUDE.md
- "bake once": фото → Seedance оживляет → текст статично поверх (ffmpeg `scale2ref`+`overlay`, `services/video-overlay.ts`) → публикация VK+IG. scheduler догружает mediaFiles+skipOverlay для scheduled STORIES. Единый дизайн-слой: `design-layer.ts` + `/media/bake-design-layer` (фото→sharp, видео→ffmpeg).

## Архитектурные решения
- Async generation: video-poller.ts (10 сек) обрабатывает video+music+photo. kieTaskId в PostgreSQL (deploy-safe)
- generating из БД (session.status), SSE session_updated, timer от kieTaskCreatedAt
- Русификация: UI на русском + translatePrompt() перед генерацией

## Паттерны
- HTTP client: fetch + httpOnly cookie + X-Tab-ID
- Auth: JWT httpOnly cookie, requireAuth middleware, refresh 1h+30d
- SSE: eventBus -> ReadableStream. session_updated для студий
- AI prompts: system prompt = base + brandContext. Русский UI + auto-translate
- Billing: AppConfig markup + usd_rub_rate. Auto-charge $transaction. ADMIN exempt
- Media library API: `{ files, hasMore, totalCount, counts }` — НЕ массив (counts только на 1-й странице)
- Publishers: getPublisher(platform, {postType, config}) — VK гибрид (PMP по флагу viaPostmypost / direct), IG→PMP, TG
- Testing: Vitest, 185 tests, 18 files. Mock Prisma via vi.hoisted()

## Уроки и грабли (плотно; детали сессий 06-19/06-20 → MEMORY-ARCHIVE.md)
- **Provider EOL ломает ТИХО:** OpenRouter `claude-3.5-haiku`→Bedrock→404 (EOL), `.catch(()=>null)` молча вернул null → дайджест без фото. Фикс: все модели ТОЛЬКО через `config.models` (одна точка). Алерт-кандидат: рост `ai_usage_logs.status≠success` / 0 вызовов ожидаемой модели.
- **Дизайн-слой = satori (НЕ Playwright):** узлы→SVG→resvg→PNG, шрифты из print-kit; **`satori-html` НЕ работает в Bun → узлы через `el()`**; эмодзи satori НЕ рисует (стрипать); `objectPosition` в satori РАБОТАЕТ (вертик. фокус кадра).
- **baked-сторис:** дизайн вшит (тег `story-design`/url `design_`) → `isBakedStory` гейтит canvas-текст (иначе дубль текста); `MediaFile.sourceMediaId` хранит оригинал для перезапекания; публикация baked = `skipOverlay:true`, `body` не перезаписывать.
- **Медиатека:** `position:sticky` НЕ липнет при скролле `<body>` (а не контейнера) → `Teleport`+`fixed`; **Set в ref ПЕРЕПРИСВАИВАТЬ** (не мутировать) — иначе computed не пересчитывается; EXIF авто-норм. только для orient>1; concurrent upload worker-pool лимит 3 (sharp/ffmpeg синхронны → бережём VPS от OOM); серверные `counts` в `/library` (не 3× O(n) filter).
- **API-факт:** подписей к сторис в API НЕТ (ни IG, ни VK) — только вшитый оверлей + VK CTA-кнопка/стикеры.
- **Единый источник публикации:** `services/publish-runner.ts` (`publishPostVersion`/`schedulePostVersion`) — для роутов И прямой публикации из дайджеста (чтобы 3 копии логики не разошлись).
