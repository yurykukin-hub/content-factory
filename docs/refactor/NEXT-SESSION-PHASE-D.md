# Промпт следующей сессии — Фаза D (запускать в PLAN-режиме)

_Скопируй блок ниже в новую сессию. Обновлено 2026-07-04 после закрытия долгов C3._

---

Продолжаем рефактор Content Factory. Я — Fable, effort high, оркестратор (механику делегирую
sonnet, тяжёлую логику/god-компоненты — opus, свой контекст держу компактным, объявляю модель
субагентов, Fable-как-оркестратор — с подтверждением).

ПЕРВЫМ ДЕЛОМ прочитай состояние:
- content-factory/docs/refactor/HANDOFF.md — базовое состояние (актуально на 2026-07-04).
- ~/.claude/plans/content-elegant-starfish.md → секция «SESSION CLOSE 2 (2026-07-04)» — что сделала
  прошлая автономная сессия (5 волн: долги C3 закрыты), остаток. ГЛАВНЫЙ источник по прошлой сессии.
- ~/.claude/plans/goal-atomic-cake.md — общий план по 3 измерениям (Фаза D — последняя).
- content-factory/lessons/01-03 — уроки (03 = унификация студий, composables-first > god-shell).
- content-factory/docs/refactor/video-models-research.md — если возьмёмся за видео-модели (нужен прод KIE-ключ).

СТАТУС РЕФАКТОРА (~90%, всё в main, НЕ задеплоено, прод content.yurykukin.ru→:3800 НЕ трогать):
Фазы A (дизайн-система), B (единый OverlaySpec+satori), C1 («Лента»), C2 (баги постинга),
C-story, C3 (СТУДИИ) — ВСЕ ✅. Долги C3 закрыты автономной сессией 04.07 (edit-image фикс,
тесты→361/361, полиш меню «Улучшить»/welcome, видео-ресёрч+персист model). Коммиты
94adf80/7abc78a/6db4513/64fd016/c52afdb + close 54f7537.

⚠️ ДО Фазы D РЕШИ: деплоить ли C3+фиксы на прод? Реальный баг edit-image («Применить» в медиатеке
не звал API) сейчас сломан В ПРОДЕ — Света/Антон им пользуются, сезон. Мой прошлый совет: СНАЧАЛА
деплой (`bash scripts/deploy.sh`, ТОЛЬКО по явной команде «деплой»), потом Фаза D на свежую голову.

ЗАДАЧА: Фаза D — последняя крупная. Спланируй порядок/скоуп в plan-режиме. 5 под-задач:
1. **Распил god-компонентов** — `MediaLibraryView.vue` (1265) + `BusinessDetailView.vue` (1030),
   поведение-сохраняюще, БЕЗ big rewrite. ⚠️ РИСК — нужны МОИ чекпоинты между кусками (НЕ автономь
   целиком). Метод как nawode `bookings.ts`/CF `daily-digest.ts`: характеризац. тесты/визуал ПЕРЕД
   выносом, байт-в-байт (sed), полная верификация после каждого шага.
2. **Удаление мёртвых view** — Идеи/Сценарии/Контент-планы (из nav убраны в C1, но роуты/файлы живы;
   `/plans` ещё достижим из «Обзора» quick-actions). Проверить 0 живых ссылок/импортов (opus-скептик)
   → удалить роуты+views+связанные компоненты → build. Низкий риск, чистый выигрыш.
3. **Консолидация design-layer** — изолированный стек (`design-layer.ts`/`useDesignLayerCanvas`/
   `DesignLayerEditor`, MediaLibrary «Дизайн-слой», НЕ на пути публикации) → в `OverlayEditor`. Не сломать медиатеку.
4. **Тултипы/хинты сквозняком** — `UiTooltip` есть с Фазы A; плейсхолдеры/подсказки где не хватает
   (a11y-гардрейл: aria-label/title на кнопки-иконки, контраст ≥4.5:1).
5. **Аналитика** — прояснить скоуп (что именно нужно; `/analytics` уже есть — KPI/разбивка/отчёты).

ПОРЯДОК (мой совет по возрастанию риска): сначала БЕЗОПАСНОЕ (№2 мёртвые view → №4 тултипы) →
потом рискованные god-splits (№1) с чекпоинтами → design-layer (№3) → аналитика (№5, после уточнения).
Спланируй, покажи план, дай списки/дизайн через скептика-opus чтоб не раздувать.

Правила: dev по HANDOFF (backend PORT=3801→dev DB :5441; vite внешний :8095 через ufw — может быть
ещё ОТКРЫТ с прошлой сессии, закрыть `ufw delete allow 8095/tcp` когда не нужен; логин
tvtest/uikit-verify-2026, бизнес «НаWоде SUP Club»). ⚠️ dev `KIE_API_KEY` пуст → генерации KIE
(видео/фото/edit-image) на dev дают 500 — это НЕ баг, ключ только в проде. ПРОД НЕ ТРОГАТЬ. Работай
фазами с чекпоинтами, Playwright before/after + консоль на каждый экран, коммит+пуш проактивно,
деплой — только по команде.
