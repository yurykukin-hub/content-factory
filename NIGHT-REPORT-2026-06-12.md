# 🌙 Ночной прогон — отчёт (2026-06-12)

Ветка: **`feature/cf-night-1`** · модель: Opus · режим: без деплоя/публикаций/деструктива.
База: 176 тестов зелёные до и после. Каждый блок проверен (фронт `vite build` + бэк `bun run test`).

---

## ✅ Сделано (5 коммитов)

### 1. `fix(ui)` — навигация и мёртвый код
- Подсветка активного пункта меню теперь работает в редакторах: `/posts/:id`, `/stories/:id` → подсвечивают «Контент»; `/businesses/:id` → «Проекты» (было: пункт не подсвечивался — терялся в меню).
- Иконка «Контент» сменена с `Film` (дублировала «Видео») на `Newspaper`.
- Удалён роут-призрак `/calendar` + `CalendarView.vue` (был мгновенный redirect).
- Удалён мёртвый `SsModeTabs.vue` + его импорт (Simple-режим скрыт).
- Англ. placeholder в SoundStudio → русский.

### 2. `fix` — баги сторис + автосейв поста + CORS  ⭐ (твоя жалоба на «глючный текст»)
- **Корень «глючного экспорта сторис» найден и вылечен:** на `/uploads/*` не было CORS-заголовка → при `crossOrigin='anonymous'` canvas «тейнтился» → экспорт падал `SecurityError`. Добавил `Access-Control-Allow-Origin` (`app.ts`). Файлы публичные — wildcard безопасен.
- StoryEditor: ждём `document.fonts.ready` перед экспортом (корректный перенос строк), явная ошибка при пустом blob, `onerror` на загрузке картинки (раньше — тихий чёрный экран).
- **PostEditor: автосейв мастер-текста** (debounce 1.5с) + сохранение при уходе со страницы. Защита от потери набранного (раньше — `confirm()` и потеря текста).

### 3. `feat(ai)` — реализованы заглушки 501
- `/ai/hashtags` (генерация хештегов) и `/ai/rewrite` (перефраз/правка по инструкции) — были `501 TODO`, теперь рабочие (Haiku, бренд-контекст, Zod + проверка доступа).

### 4. `feat(competitors)` — мониторинг конкурентов MVP  ⭐ (ты хотел «раньше»)
- Модели `CompetitorAccount` + `CompetitorPost` + миграция (создана, **не применена** — см. ниже).
- `competitor-poller.ts`: раз в день читает VK `wall.get` по пабликам твоим же токеном (scope не нужен), считает ER, помечает «залетевшие» (ER ≥ медиана×2).
- Утренний дайджест теперь получает блок «что залетело у конкурентов за 7 дней» → агент черпает идеи.
- Сид 8 реальных пабликов (SUP Выборг/ЛО/СПб/Карелия).
- Ручной триггер: `POST /api/auto-posts/collect-competitors` (admin).

### 5. `refactor(logging)` — console.* → структурный логгер
- `vk.ts` (14), `vk-oauth.ts` (4), `scheduler.ts` (10) → JSON-логи. Лучше видно падения публикации в проде. (Сделано sonnet-субагентом, проверено.)

---

## ⏸ Отложено в «нужен ты» (сознательно не трогал без присмотра)

- **«В пост» из студий** (мост студия→публикация, топ-UX-проблема). Building blocks подтверждены: эндпоинт `POST /media/:id/attach` есть, results несут `mediaFileId`. НО это цепочка через 3 студии с разным маппингом — полноценная Фаза-2-фича, не «quick win». Готово к чистой реализации с тобой.
- **3-я (SVG) реализация наложения текста в `vk.ts`** — НЕ удалял: это редкий фолбэк (если потеряется pre-rendered слой — пост уйдёт без текста). Часть унификации Design Layer (Фаза 1, с тобой).
- **Большой Design Layer Engine + декомпозиция StoryEditorView** — архитектура, с тобой.
- `confirm()` → модалки (9 мест) — не успел, низкий риск, можно добить.

---

## ⚠️ ВАЖНО перед деплоем

1. **Миграция конкурентов НЕ применена** (создан только файл `prisma/migrations/20260612032421_add_competitor_monitoring/`). Применится автоматически при `deploy.sh` (он гонит `prisma migrate deploy`). Проверь файл перед деплоем.
2. **После применения миграции** — засей конкурентов в проде: `bun src/seed-competitors.ts` (внутри backend-контейнера). Это создаст 8 пабликов И включит модуль (`competitor_monitor_enabled=true`).
3. Модуль конкурентов **opt-in** — без сида/конфига поллер просто ничего не делает (безопасно).
4. **НЕ задеплоено, НЕ опубликовано ничего.** Ветка ждёт твоего ревью.

---

## Как проверить (когда проснёшься)

```bash
git -C /home/dev/projects/content-factory checkout feature/cf-night-1
git log --oneline -6              # 5 ночных коммитов
cd backend && bun run test        # 176 зелёных
cd .. && bun run build            # фронт собирается
```

Дальше: ревью diff → если ок, мердж в main → `deploy.sh` (применит миграцию) → `seed-competitors.ts` в проде.

## Открытые вопросы (из плана) — нужно от тебя
1. Метрика OAuth-токен НаWоде (замкнуть воронку пост→бронь).
2. VK-переавторизация со scope `stats` (охваты/сторис-стат).
3. Проверить список 8 пабликов конкурентов (или дать свои).
4. Бренд-ассеты (логотип/цвета/шрифт) для шаблонов дизайн-слоя.
5. С чего начать Фазу 1: багфиксы (часть уже сделана) vs сборка Design Layer Engine.
